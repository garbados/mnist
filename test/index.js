var mnist = require('../');

describe('mnist', function () {
  before(function () {

  });

  it('should retrieve ranges of images as lists of [i][j] matrices of integers between 0 and 255 inclusive', function () {
    // TODO
  });
});